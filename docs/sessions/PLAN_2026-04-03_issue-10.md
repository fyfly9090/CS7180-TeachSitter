# PLAN — Issue #10: Teacher Booking Requests Page

**Date:** 2026-04-03
**Issue:** #10 — Teacher booking requests page (UI)

## Components

### 1. Teacher Layout (`app/teacher/layout.tsx`)

- Extract navbar + mobile bottom nav from dashboard into shared layout
- Desktop: fixed top bar with logo, nav links (Dashboard, Booking Requests, My Profile), notifications, avatar
- Mobile: fixed bottom nav with 3 tabs
- Active link highlighting via `usePathname()`

### 2. Requests Page (`app/teacher/requests/page.tsx`)

- Client component (`"use client"`)
- Data: fetch from `/api/teachers/me/bookings` on mount
- States: loading, error, confirmed[], pending[]

### Page Layout

```
[Stats Cards: 2-col grid]
  - Confirmed Bookings count (primary icon)
  - Pending Requests count (secondary icon)

[Main Grid: lg:grid-cols-12]
  Left (lg:col-span-8): Upcoming Bookings
    - Each row: initials avatar, parent name, "Confirmed" tag, date range, Details button
    - Empty state: "No upcoming bookings."

  Right (lg:col-span-4): Pending Requests sidebar
    - Header with badge count
    - Each card: initials avatar, parent name, date range, message excerpt, Accept/Decline
    - Empty state: "No pending requests."
```

### Optimistic UI

1. On Accept: remove from pending, add to confirmed, call PATCH
2. On Decline: remove from pending, call PATCH
3. On PATCH failure: rollback both arrays, show error banner

### 3. Unit Test (`__tests__/teacher-requests-page.test.tsx`)

- Mock global fetch
- Test loading spinner renders
- Test confirmed/pending sections render after data loads
- Test Accept button triggers PATCH with "confirmed"
- Test Decline button triggers PATCH with "declined"

## Implementation Order

1. Create teacher layout (shared navbar)
2. Write tests (RED)
3. Implement requests page (GREEN)
4. Polish if needed (REFACTOR)
